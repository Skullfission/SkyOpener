# Music

Drop your original tracks here. The game will auto-load `SkyOpener.mp3` (or `.ogg` / `.wav`)
when you click **Launch** if it exists. You can also pick any audio file from the start
overlay's file picker.
